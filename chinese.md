# 为了避免看不懂题目，先做了翻译

考古学研究需要准确复原破碎的文物（如陶器），以揭示文明并记录历史。手动复原破碎的陶器既耗时又需要体力操作。为了应对这一挑战，对于那些充满热情且善于思考的人而言，你们将利用在计算机视觉和人工智能方面的知识，构建一个生成模型，该模型将帮助考古学家从陶器碎片中复原出完整的形状。

在这项任务中，考古学家提供了一个陶器数据集vox_pottery.zip。这是一个描述11种类型的体素数据集。在以下描述中，我们将引导您了解该数据集，并逐步教授您如何基于3D自编码器生成对抗网络（Generative Adversarial Network, GAN）构建一个体素补全模型。您的目标是成功训练并测试其复原效果。

这些陶瓷根据其形状被分为十一类。这些分类考虑了口沿、颈部、主体、底部和把手的形式，以及它们尺寸之间的相对比例。其中九类对应的是封闭式陶器形状，另外两类则属于开放式陶器。

注意事项：

数据集采用“.vox”后缀格式，表示它遵循MagicaVoxel格式，这种格式能够紧凑地表示数据，并且便于使用pyvox.parser库进行处理。
每个文件包含一个完整的陶瓷器皿，分辨率为64×64×64。读取后，您将获得一个numpy.ndarray类型的数据，其中每个位置记录了碎片的标签。
数据集包含最多10种类型的碎片，编号从1到10，其中0表示一个“空”位置。

1) 尝试可视化数据集（10分）
对于您的第一次尝试，请完成utils/visualize.py中的以下任务：

在给定的分辨率（可能是32×32×32）下，成功读取一个体素数据，并在__read_vox__函数中熟悉这些数据。考虑到内存消耗较大，我们可能最初会在较低的分辨率下实现生成对抗网络（GAN）。
实现__read_vox_frag__函数，从体素数据中读取指定索引fragment_idx处的碎片。一个简单的方法是将不匹配给定标签的体素设置为零。
修改plot函数并实现plot_frag函数，分别用于可视化整个陶器或陶器的某一个特定部分。
实现plot_join函数用于最终分析。此函数接收两个体素数据并将它们组合在一起，这对于可视化您的模型生成的碎片并将其与输入数据进行匹配非常有用。
如果您成功完成了上述代码实现，您应该获得以下结果和功能（注意：您需要自己实现单元测试；创建一个.ipynb文件来检查您的实现的有效性是很有效的）：

您应该能够读取体素文件并理解数据的结构。
您应该能够通过指定碎片索引来提取并可视化特定的碎片。
您应该能够可视化整个陶器或陶器的某个部分。
您应该能够使用plot_join函数将模型生成的碎片与原始输入数据进行可视化对比。
为了验证您的实现，您可以创建一个Jupyter Notebook文件，并在其中编写测试代码来检查每个函数的功能是否按预期工作。

2. 实现 FragmentDataset.py
通过上述尝试，您已经熟悉了数据集格式（如果您觉得它无趣或具有挑战性，现在换任务还不晚🤣）。为了利用这些数据来训练您即将设计的网络，第一步是构建一个Pythonclass，负责处理数据的读取和处理，并为其准备DataLoader类所需的数据格式。

考虑到我们的目标是从陶瓷碎片中重建整个陶器的形状，我们将其命名为FragmentDataset再合适不过了。这个名字表明，数据集将在这个类中进行进一步的处理，包括随机或特定地提取训练所需的碎片。该类继承自torch.utils.data.Dataset类。您可以参考其源代码来确定需要实现的部分。

2）实现 Dataset 类（15分）
按照utils/FragmentDataset.py中的说明，实现以下函数：

__init__ 函数：您必须实现__init__函数，并初始化可能在后续过程中有用的属性。
__getitem__ 方法：这个方法将在DataLoader中被调用。请相应地实现它。
__getitem_specific_frag__ 函数：为了评估目的，您可能需要指定要选择的碎片类型。实现此函数以实现该功能。
在上述函数中分别调用__select_fragment__ 和 __select_fragment_specific__ 方法。
实现细节：
__init__ 函数：
初始化数据集路径、分辨率、可能的碎片类型等属性。
加载数据集，可能包括读取所有.vox文件并存储相关信息。
__getitem__ 方法：
根据索引从数据集中随机选择一个样本（整个陶器）。
使用__select_fragment__方法随机选择一个或多个碎片。
返回碎片及其对应的完整陶器形状（作为标签）。
__getitem_specific_frag__ 方法：
根据指定的碎片类型选择一个样本。
使用__select_fragment_specific__方法选择特定类型的碎片。
返回特定碎片及其对应的完整陶器形状（作为标签）。
__select_fragment__ 和 __select_fragment_specific__ 方法：
这两个方法负责从陶器中选择碎片。
__select_fragment__ 方法随机选择碎片。
__select_fragment_specific__ 方法根据指定类型选择碎片。
注意事项：
确保您的实现能够处理不同大小和形状的碎片。
考虑内存和性能优化，特别是在处理高分辨率数据时。
编写适当的注释和文档，以便其他开发人员能够理解您的代码。
完成这些实现后，您应该能够使用DataLoader类来迭代地加载和处理您的数据集，为训练GAN模型做好准备。

3. 构建一个基础生成对抗网络（GAN）
一个典型的生成对抗网络（GAN）框架由一个生成器（G）和一个判别器（D）组成。生成器G的目标是生成逼真的工件，而判别器D则学习区分真实数据p_d(x)和生成的样本。在我们的场景中，z是一个体素化的输入碎片（我们建议您从32 × 32 × 32的体积开始）。生成器函数G(z)将这个碎片映射到完整的体素化陶器的数据空间，从而创建了一个自编码生成对抗网络（AE-GAN）。

在AE-GAN中，生成器G包括编码器，这些编码器经过训练，可以将碎片映射到潜在空间，而解码器则可以将潜在空间中的点（w）映射回完整的陶器。通过迭代调整，生成器G学会在判别器D中产生误分类。在这里，x是一个三维二进制数组，代表体素化的陶器几何形状。D(G(z))是生成器G的输出被判别为来自3D数据集中的真实工件的概率。

判别器D最大化log(D(x))，即正确分类真实陶瓷的概率，而生成器G最小化log(1−D(G(z)))，即判别器D识别生成输出的概率。您将要实现的模型是一个自编码生成对抗网络，其中生成器是一个自编码网络，包括编码器Encode(z)→w和解码器Decode(w)→x′。为了训练判别器，我们使用D(y)，但对于生成的样本，这里的y=z+x′可能是一个误导或简化，因为在标准的GAN训练中，判别器通常直接接收生成的样本x′或真实样本x作为输入，而不是它们的某种组合或加法。因此，在训练判别器时，您应该直接使用生成的陶器x′或真实陶器x。

3) 实现3D自编码生成对抗网络（AE-GAN）（20分）
在实现生成对抗网络（GAN）的各个模块时，我们建议您遵循自编码器结构中流行的“卷积-批量归一化-激活”（Conv-BatchNorm-Activate）或“卷积-批量归一化-最大池化-激活”（Conv-BatchNorm-Maxpooling-Activate）范式。您可以轻松地搜索到众多2D自编码器仓库作为参考。

在开始之前，您可能会好奇卷积核大小、步幅和填充数的选择。以2D数据为例，卷积后图像的维度遵循以下公式：

new_len= (len−kernel_size+2×padding) / stride +1

因此，对于32分辨率或64分辨率，您可以选择合适的卷积核大小、填充和步幅。

以下指令是您将在utils/model.py中执行的操作：

选择一种范式，并确定卷积核大小、填充和步幅。此外，您还应该了解瓶颈潜在空间z的维度。
构建__init__方法来初始化超参数。您的模型应该兼容32分辨率或64分辨率的数据。
根据手册和Python文件中的指导，在__init__方法中构建模块。
在Generator（生成器）和Discriminator（判别器）中实现__forward__函数。

为了减轻参数调整的负担，我们提供了一组可能的超参数：

针对此体素补全任务，我们提供了几种可能的评估指标：Dice相似系数（DSC）[4]；Jaccard距离[5]和均方误差（MSE）。

给定A：生成的体素集合，和B：真实的陶瓷体素集合，DSC的计算公式为：
DSC= 2∣A∩B∣/(∣A∣+∣B∣) ，其中∣A∣和∣B∣分别表示陶瓷体素的数量。该公式的值介于0和1之间，其中1表示最大相似性，0表示最小相似性。

Jaccard距离与DSC一样，是用于估计数据集不相似度的一种统计量，但与DSC不同的是，Jaccard距离满足三角形不等式。Jaccard距离定义为：
JD(A,B)= (∣A∪B∣−∣A∩B∣) /∣A∪B∣
​，其中A是包含生成的陶瓷体素化几何形状的三维二进制数组，B是真实的陶瓷体素集合。该公式的值也介于0和1之间，但与DSC不同的是，1表示最小相似性，0表示最大相似性。

MSE是二维和三维分类及重建问题中常用的质量估计量，我们在此不再赘述。
如果您发现难以实现上述指标，可以设计自己的指标并证明其有效性。

4) 实现training.py（25分）
根据training.py中的指令，实现整个训练过程。
确保您为判别器（Discriminator）和生成器（Generator）正确计算了得分函数并更新了梯度。
此外，尝试实现一个测试函数，利用测试数据集和测试数据加载器来验证当前的训练质量。您可能需要实现多个评估指标，并最终对您的努力进行定量和定性分析。

5. 提高分辨率
如果您已经进行到这一步，恭喜您！您已经成功使用构建的生成器恢复了陶器碎片！现在让我们继续前进，尝试在64×64×64的更高分辨率下进行操作！

5) 在64^3分辨率下训练和测试您的模型（5分）
在更高分辨率的数据集上重新训练您的模型。
进行全面的定量分析。
比较标签和碎片百分比之间的可视化结果。

6. （可选）改进
您可能已经注意到，在更高分辨率下，生成的碎片可能会出现变形和扭曲。这里指出，引入法向量通常有助于提高生成模型重建的质量。设计更侧重于法向量或其他方面的损失函数也是有益的。此外，切换到更强大的模型主干也可以提供帮助。

因此，如果您愿意挑战自己，可以完成以下三个部分：

6) 对朴素GAN的改进（25分）
尝试基于体素化数据集引入表面法线。
除了基本的GAN损失之外，推导出有效的损失函数。
引入更有效的GAN结构或尝试其他生成模型。

报告与演示：
请撰写一份详细的报告，总结你们小组的所有工作，并展示所有已完成的实验。报告中必须明确列出团队成员的姓名和学生ID，以及分配给每个学生的任务。在最后阶段，我们将组织两次课堂演示，因此请确保每组学生尽早准备。

对于更多基本信息，您可以参考参考文献中列出的论文，并进行互联网搜索。我们鼓励小组内讨论，因此请尽量减少向助教寻求帮助的频率，并努力在小组内独立完成任务。